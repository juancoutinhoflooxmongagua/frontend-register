import React from "react";
import { BrowserRouter } from "react-router-dom";

export default (props) => (
  <BrowserRouter>
    <div className="app">
      <h1>Oi</h1>
    </div>
  </BrowserRouter>
);